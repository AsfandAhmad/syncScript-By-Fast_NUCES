# ──────────────────────────────────────────────
# SyncScript — Frontend Environment Variables
# Copy this to .env.local and fill in real values
# ──────────────────────────────────────────────

# Supabase (public — safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key

# Supabase (server-only — NEVER expose in client bundle)
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Upstash Redis (optional — only needed if backend caching is consumed from Next.js API routes)
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=AX...your-token
